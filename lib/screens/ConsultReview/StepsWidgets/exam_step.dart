import 'package:flutter/material.dart';

class ExamStep extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Text("Exam Step"),
    );
  }
}

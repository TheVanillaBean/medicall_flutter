import 'package:flutter/material.dart';

class TreatmentStep extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Text("Treatment Step"),
    );
  }
}
